# TASK-020-04: スロット同期方向の修正（縦方向から横方向へ）と Gap 定義の調整

## 概要
REQ-020 の実装において、スロットの同期方向が誤って縦方向（Y軸オフセット）になっていた不備を修正し、ユーザーの期待通り横方向（X軸オフセット、Y軸一致）に同期するように変更する。

## 工程
- [ ] `src/store/roiStore.ts` の `gaps` 定義の修正
  - `slotGapY` を削除し、`slotGapX` (スロット間の横間隔) を追加。
- [ ] `syncProfile` ロジックの修正
  - スロットの同期を「X軸オフセット、Y軸一致」に変更。
  - スキルの同期は「Y軸オフセット、X軸一致」であることを維持。
- [ ] `DEFAULT_PROFILE` のスロット初期座標を横並びに修正。
- [ ] `src/store/roiStore.test.ts` のスロット同期テストを修正。
- [ ] `src/components/roi/ROICalibrator.tsx` の UI 連動（Gap調整パネル）を新定義に合わせて修正。

## 完了条件
- スロットをドラッグした際、3つのスロットが横一列を保って同期移動すること。
- スロット間の横間隔を `slotGapX` で調整できること。
- 全てのユニットテストがパスすること。
