# TASK-020-03: InteractiveCanvas.tsx のドラッグ連動と検証

## 概要
キャンバス上のインタラクションを全枠同期に対応させ、最終的な動作検証を行う。

## 工程
- [ ] `InteractiveCanvas.tsx` の `handleMouseMove` 修正
  - ドラッグ時に `updateRelativeRect` を通じて全枠が同期して動くことを確認。
  - どのID（1, 2, 3）を起点にしてドラッグを開始した場合でも、相対位置が崩れないように調整。
- [ ] `renderBox` における全枠表示の視認性確認
- [ ] テストプランに基づく全項目の実機検証

## 完了条件
- どの枠を操作してもグループ全体が追従すること。
- デザイン、操作感ともに MHW スタイルのプレミアムな品質を満たしていること。
