# TASK-027-03: roiStore.test.ts によるロジック検証

## 概要
刷新した `syncProfile` ロジックが、意図通り 4 つのグループを独立して同期できているかをテストコードで検証します。

## 対象ファイル
- `src/store/roiStore.test.ts` (NEW or MODIFY)

## 実装詳細
1. `syncProfile` の単体テストを記述。
2. 以下のケースを検証する。
   - `slotIconGapX` を変更した際、`slots[].icon` のみが動くこと。
   - `slotLevelGapX` を変更した際、`slots[].level` のみが動くこと。
   - 同様にスキル側の2グループについても検証。
   - プライマリ要素（ID:0）を移動させた際、同グループの ID:1, 2 が追従すること。

## 完了条件
- `npm test` を実行し、すべてのテストがパスすること。
