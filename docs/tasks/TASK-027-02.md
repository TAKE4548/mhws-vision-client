# TASK-027-02: ROICalibrator.tsx の UI マッピング修正

## 概要
`roiStore.ts` での Gap キー変更に伴い、`ROICalibrator.tsx` の UI 表示と更新ロジックを修正します。

## 対象ファイル
- `src/components/ROICalibrator.tsx`

## 実装詳細
1. `targetMap` を新しい Gap キーに合わせて更新する。
   - `slot_icon` -> `slotIconGapX`
   - `slot_level` -> `slotLevelGapX`
   - `skill_name` -> `skillNameGapY`
   - `skill_level` -> `skillLevelGapY`
2. `Gap Adjustment` パネル内のラベルやツールチップ（あれば）が正しい対象を指すように調整。

## 完了条件
- 各カテゴリ（Slot Icons / Slot Levels 等）を選択した際、対応する Gap 調整用スライダーが表示され、操作内容が正しくストアに反映されること。
