# TASK-024-04: サンプルコンポーネントテストによる動作確認

## 概要
React Testing Library が正しくセットアップされたことを確認するため、既存コンポーネントに対する簡単なテストを作成します。

## 完了条件
- [ ] `src/components/common/NumericalAdjuster.test.tsx` が作成され、テストがパスする。
- [ ] DOM に対するアサーション（例: `toBeInTheDocument`）が正常に機能している。

## 対象ファイル
- `src/components/common/NumericalAdjuster.test.tsx` [NEW]

## 実装上の注意
- `NumericalAdjuster` は数値の増減を行うシンプルなコンポーネントであるため、基盤検証に適している。
- レンダリング、ボタンクリック時のコールバック発火などをテストする。
