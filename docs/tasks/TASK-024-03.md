# TASK-024-03: テストセットアップファイルの作成と既存テストの調整

## 概要
テスト実行前のグローバルな初期化を行う `setup.ts` を作成し、既存の `roiStore.test.ts` が正常に実行できるようにします。

## 完了条件
- [ ] `src/test/setup.ts` が作成され、`@testing-library/jest-dom` がインポートされている。
- [ ] `src/store/roiStore.test.ts` が Vitest 環境でエラーなく実行できる状態になっている。

## 対象ファイル
- `src/test/setup.ts` [NEW]
- `src/store/roiStore.test.ts`

## 実装上の注意
- `setup.ts` では、将来的に必要となる可能性が高い `ResizeObserver` などのブラウザ特有の API の型やスタブを定義しておく。
- `roiStore.test.ts` は既に存在するため、グローバル変数の競合やインポート不足がないかを確認する。
