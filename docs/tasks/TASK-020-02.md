# TASK-020-02: ROICalibrator.tsx パネルのリデザイン

## 概要
ユーザーからの要望に基づき、操作パネルをカテゴリ単位の選択と間隔（Gap）調整に特化したUIに刷新する。

## 工程
- [ ] `Select ROI Target` セクションのボタン構成変更
  - `SLOT ICONS`, `SLOT LEVELS`, `SKILL NAMES`, `SKILL LEVELS`, `RARITY` の 5 項目に集約。
- [ ] `setActiveTarget` 呼び出し時の ID 固定化（グループ全体を指すため、常に 0 等を指定）
- [ ] カテゴリ選択時に表示される `Gap Controller` コンポーネントの実装
  - スライダー、数値入力、±1px微調整ボタンの配置。
- [ ] スタイル調整（MHW HUDスタイルに準拠）

## 完了条件
- 各カテゴリを選択した際、適切な Gap 調整 UI が表示されること。
- 個別の Slot 1/2/3 を意識せずに、カテゴリ単位で操作が完結すること。
