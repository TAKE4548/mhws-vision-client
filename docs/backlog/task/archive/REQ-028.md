# Task Card: REQ-028 Implementation

## 1. OpenAPI Specification Adjustment
- **Files**: `<PROJECT_ROOT>\mhws-vision-server\docs\system\openapi.yaml`
- **Actions**:
    - 全てのエンドポイント（特に `analyze`, `vision` 関連）に `tags` が付与されているか再点検し、不足があれば補完する。
    - `multipart/form-data` を使用するエンドポイント (`/analyze/video`, `/vision/prepare`) の定義が正しいか確認する。

## 2. API Client Mutator Fix
- **Files**: `src/lib/api-client.ts`
- **Actions**:
    - `customInstance` 関数内で、`config.data` が `FormData` のインスタンスである場合、`config.headers` から `Content-Type` を削除するロジックを追加する。
    - ```typescript
      if (config.data instanceof FormData) {
        delete config.headers['Content-Type'];
      }
      ```

## 3. Client Code Regeneration & UI Verification
- **Files**: `src/api/generated/`
- **Actions**:
    - `npm run gen:api` (または `npx orval`) を実行し、コードを再生成する。
    - `visionStore.ts` で `createAnalysisJob` や `startAnalysis` の呼び出し箇所が再生成されたコードと整合しているか確認する。
    - ブラウザで動画アップロードを試行し、`boundary` が付与された状態で実サーバにリクエストが飛ぶことを確認する。