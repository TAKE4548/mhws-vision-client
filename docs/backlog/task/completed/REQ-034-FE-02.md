# TASK-REQ-034-FE-02: WaveformGraph コンポーネントの実装と統合

## 1. ターゲット識別
- **対象ファイル**: 
    - `src/components/vision/WaveformGraph.tsx` (新規)
    - `src/components/vision/TalismanDetailsModal.tsx`
- **対象シンボル**: `WaveformGraph`, `TalismanDetailsModal`

## 2. 密閉コンテキスト (Hermetic Context)

### 2.1 必須の型定義
```typescript
interface WaveformGraphProps {
  waveform: number[];
  threshold: number;
  className?: string;
}
```

### 2.2 既存の対象コード (TalismanDetailsModal.tsx)
```typescript
// 既存の mock renderWaveform
const renderWaveform = (seed: number) => {
  const points = Array.from({ length: 20 }, (_, i) => ({
    x: i * 5,
    y: 10 + Math.sin((i + seed) * 0.8) * 8 + Math.random() * 4
  }));
  return (
    <svg className="w-full h-8 opacity-40" viewBox="0 0 100 20">
      <polyline
        fill="none"
        stroke="url(#neonGradient)"
        strokeWidth="1"
        points={points.map(p => `${p.x},${p.y}`).join(' ')}
      />
      {/* ... */}
    </svg>
  );
};
```

## 3. 実装要件 (Implementation Logic)
- [x] `src/components/vision/WaveformGraph.tsx` を新規作成する。
    - 受取った `waveform` (0.0~1.0) を SVG の高さに合わせてスケールして `polyline` で描画する。
    - `threshold` の位置に水平線を描画する。
    - `viewBox="0 0 100 40"` 程度を基準にし、アスペクト比を維持する。
- [x] `TalismanDetailsModal.tsx` で `WaveformGraph` をインポートし、既存の `renderWaveform` (mock) を置換する。
- [x] `slots_debug_data` が存在する場合のみグラフを表示し、存在しない場合は既存のプレースホルダーを維持するか空表示にする。

## 4. 検証手順 (AC)
- [x] `npm run type-check` でエラーがないこと。
- [x] `TalismanDetailsModal` を開き、スロット部分に波形が表示されることを確認する。
- [x] 波形の形が `slots_debug_data` の値に追従していることを確認する。
