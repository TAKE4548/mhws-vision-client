# TASK-REQ-034-FE-01: API モデルの更新 (slots_debug_data / crops)

## 1. ターゲット識別
- **対象ファイル**: 
    - `src/api/generated/model/talismanOut.ts`
    - `src/api/generated/model/skillInfo.ts`
- **対象シンボル**: `TalismanOut`, `SkillInfo`
- **行番号目安**: 全体

## 2. 密閉コンテキスト (Hermetic Context)

### 2.1 必須の型定義 (OpenAPI 抽出)
```typescript
// TalismanOut への追加分
slots_debug_data?: {
  waveform: number[];
  threshold: number;
}[];

// SkillInfo への追加分
crop_b64?: string | null;
lv_crop_b64?: string | null;
```

### 2.2 既存の対象コード
```typescript
// src/api/generated/model/talismanOut.ts
export interface TalismanOut {
  capture_id: string;
  confidence: number;
  image_url?: string | null;
  rarity: ConfidenceValueInt;
  skills: SkillInfo[];
  slots: ConfidenceValueListInt;
  validation_status: ValidationStatus;
}

// src/api/generated/model/skillInfo.ts
export interface SkillInfo {
  confidence?: number;
  level: number;
  name: string;
}
```

## 3. 実装要件 (Implementation Logic)
- [x] `src/api/generated/model/talismanOut.ts` を編集し、`slots_debug_data` フィールドを追加する。
- [x] `src/api/generated/model/skillInfo.ts` を編集し、`crop_b64` と `lv_crop_b64` フィールドを追加する。
- [x] `src/api/generated/model/index.ts` (バレルファイル) に変更がないか確認し、必要に応じてエクスポートを維持する。

## 4. 検証手順 (AC)
- [x] `npm run type-check` を実行し、既存のコード（`TalismanDetailsModal.tsx` 等）で型エラーが発生しないことを確認する。
- [x] エディタ上で `Talisman` 型のオブジェクトに `slots_debug_data` プロパティが認識されることを確認する。
