# TASK-REQ-034-FE-03: デバッグ情報のアコーディオン UI とクロップ画像の表示

## 1. ターゲット識別
- **対象ファイル**: 
    - `src/components/vision/TalismanDetailsModal.tsx`
- **対象シンボル**: `TalismanDetailsModal`

## 2. 密閉コンテキスト (Hermetic Context)

### 2.1 必須の型定義
```typescript
// SkillInfo に crop_b64 が含まれていることが前提 (Task 01)
```

### 2.2 既存の対象コード
```typescript
// Skill identification section
{(edited.skills || []).map((skill, idx) => (
  <div key={idx} className="flex gap-4 items-end group/field bg-white/5 p-4 rounded-tech relative">
    {/* ... inputs ... */}
  </div>
))}
```

## 3. 実装要件 (Implementation Logic)
- [x] `TalismanDetailsModal` に `showDebug` ステートを追加する (初期値: `false`)。
- [x] ヘッダー付近または各セクションに `showDebug` を切り替えるトグルボタンを追加する。
- [x] `showDebug` が `true` の時のみ、以下の要素を表示する:
    - 各スキルの入力欄の横に、`skill.crop_b64` があれば `img` タグで表示する。
    - 各スロットの `WaveformGraph` を表示する（現在は常時表示の可能性があるため、トグル制御下に置く）。
- [x] アコーディオンの展開には `animate-in slide-in-from-top` 等の Tailwind アニメーションを使用し、スムーズな表示を実現する。

## 4. 検証手順 (AC)
- [x] モーダルを開いた際、デフォルトでクロップ画像や詳細グラフが表示されていないこと。
- [x] トグルを ON にした際、スキル名の横に抽出画像が表示されること。
- [x] トグルを ON にした際、スロットの波形グラフが表示されること。
